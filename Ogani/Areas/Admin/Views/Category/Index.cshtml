@model List<CategoryDto>

<div class="card">
    <h5 class="card-header">Categories</h5>
    <div class="table-responsive text-nowrap">
        <button type="submit" class="btn btn-primary">Create</button>

        <table class="table table-dark">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Name</th>
                    <th>Image</th>
                    <th>Update</th>
                    <th>Delete</th>
                </tr>
            </thead>
            <tbody class="table-border-bottom-0">
                @foreach (var category in Model)
                {
                    <tr>
                        <td><i class="fab fa-angular fa-lg text-danger me-3"></i> <strong>@category.Id</strong></td>
                        <td>@category.Name</td>
                        <td>
                           <img src="@category.ImageUrl" width="100px" alt="Alternate Text" />
                        </td>
                        <td><button asp-controller="Category" asp-action="Update" class="badge bg-label-primary me-1">Update</button></td>
                        <td>
                            <a asp-controller="Category" asp-action="Delete" asp-route-id="@category.Id" class="badge bg-label-primary me-1">Delete</a>
                        </td>
                    </tr>

                }
               
            </tbody>
        </table>
    </div>
</div>